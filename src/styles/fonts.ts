// Instead of using Next.js font module, export objects with className properties
// that can be used similarly in the JSX

export const sfProDisplay = {
  variable: 'font-sf-pro-display',
  // Other properties as needed
};

export const jetbrainsMono = {
  variable: 'font-jetbrains-mono',
  // Other properties as needed
};
